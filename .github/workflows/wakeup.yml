name: 'wake up'

on:
  watch:
    types: started
  schedule:
    - cron: '*/20 0-15 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id
    steps:
      - name: 'wakeup'
        run: |
          ifconfig
          curl https://info.mhuig.top
          curl https://s1uxuqjzzpqr.leanapp.cn
          curl https://zhm0aoiqwqwt.leanapp.cn
